<Layouts.app flash={@flash}>
  <%= case @live_action do %>
    <% :index -> %>
      <CepheusWeb.DashboardHTML.index_view
        devices={@devices}
        summary_by_serial={@summary_by_serial}
        open_event_counts={@open_event_counts}
        window={@window}
        windows={@windows}
        last_loaded_at={@last_loaded_at}
      />
    <% :show -> %>
      <CepheusWeb.DashboardHTML.show_view
        device={@device}
        tasks={@tasks}
        summary={@summary}
        kpis={@kpis}
        events={@events}
        findings={@findings}
        window={@window}
        windows={@windows}
        last_loaded_at={@last_loaded_at}
      />
  <% end %>
</Layouts.app>
